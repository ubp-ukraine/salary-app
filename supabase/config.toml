# Supabase config для payments-app.
# ВАЖЛИВО: цей інстанс піднімається НА ТІЙ САМІЙ VM, що й основний проєкт (WMS),
# тому project_id ІНШИЙ і порти взяті з ОКРЕМОГО блоку 49000-49007 —
# навмисно далеко від основного стеку (той займає 5432x, edge inspector 8083),
# щоб нічого не перетиналось.
#
#   Основний (WMS)      payments-app (цей)
#   api      54321  →   49000   ← сюди веде НОВИЙ Cloudflare Tunnel
#   db       54322  →   49001
#   shadow   54320  →   49002
#   pooler   54329  →   49003
#   studio   54323  →   49004
#   inbucket 54324  →   49005
#   analytics 54327 →   49006
#   edge insp 8083  →   49007
#
# Docker-контейнери namespaced за project_id, тож два стеки живуть поруч.

project_id = "salary-app"

[api]
enabled = true
port = 49000
schemas = ["public", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000
# Публічний HTTPS URL API (Kong за Cloudflare Tunnel `api-ubp` → http://10.18.10.50:49000).
# Домен активний і резолвиться, тож health-check при `supabase start` пройде.
external_url = "https://api.erp-ubpukraine-pay.com"

[api.tls]
enabled = false

[db]
port = 49001
shadow_port = 49002
health_timeout = "2m"
# Має збігатися з major_version основного стеку на цій VM.
major_version = 17

[db.pooler]
enabled = false
port = 49003
pool_mode = "transaction"
default_pool_size = 20
max_client_conn = 100

[db.migrations]
enabled = true
schema_paths = []

[db.seed]
enabled = false
sql_paths = ["./seed.sql"]

[realtime]
# Локально контейнер realtime часто нестабільний; тут він не потрібен.
enabled = false

[studio]
enabled = true
port = 49004
api_url = "http://127.0.0.1"

[inbucket]
enabled = true
port = 49005

[storage]
enabled = true
file_size_limit = "50MiB"

[storage.s3_protocol]
enabled = true

[storage.analytics]
enabled = false

[storage.vector]
enabled = false

[auth]
enabled = true
# Базовий URL ФРОНТА (де відкривається застосунок). Наразі — Cloudflare Worker.
site_url = "https://payments-app.crm-a10.workers.dev"
# Точні URL, дозволені для редіректів після авторизації (dev + прод фронта).
additional_redirect_urls = [
  "http://localhost:5174",
  "http://127.0.0.1:5174",
  "https://payments-app.crm-a10.workers.dev",
  "https://erp-ubpukraine-pay.com",
  "https://www.erp-ubpukraine-pay.com",
]
jwt_expiry = 3600
# Issuer посилань Auth (recovery/verify). Той самий api-домен за тунелем.
jwt_issuer = "https://api.erp-ubpukraine-pay.com/auth/v1"
enable_refresh_token_rotation = true
refresh_token_reuse_interval = 10
enable_signup = true
enable_anonymous_sign_ins = false
enable_manual_linking = false
minimum_password_length = 6
password_requirements = ""

[auth.email]
enable_signup = true
double_confirm_changes = true
# Немає SMTP локально — не вимагаємо підтвердження email, щоб логін працював одразу.
enable_confirmations = false
secure_password_change = false
max_frequency = "1s"
otp_length = 6
otp_expiry = 3600

[auth.sms]
enable_signup = false
enable_confirmations = false

[edge_runtime]
enabled = true
policy = "per_worker"
inspector_port = 49007
deno_version = 2

[analytics]
enabled = false
port = 49006
backend = "postgres"
